# Active upstream - this file is dynamically rewritten by the deploy script.
# It always points to whichever slot (blue or green) is currently live.
upstream vitalink_backend {
    server vitalink-blue:3000;
}
